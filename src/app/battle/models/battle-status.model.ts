export type BattleStatus = 'PENDING' | 'RUNNING' | 'COMPLETED' | 'CANCELLED';

export enum BattleStatuses {
  pending = 'PENDING',
  running = 'RUNNING',
  completed = 'COMPLETED',
  cancelled = 'CANCELLED',
}
